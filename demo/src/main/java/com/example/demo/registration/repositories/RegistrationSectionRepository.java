package com.example.demo.registration.repositories;

import com.example.demo.registration.models.RegistrationSection;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RegistrationSectionRepository extends JpaRepository<RegistrationSection, Long> {
}
